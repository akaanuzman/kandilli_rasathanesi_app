name: kandilli_rasathanesi_app
description: A new Flutter project.

publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: '>=2.18.5 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Visual Elements
  cupertino_icons: ^1.0.2
  animate_do: ^3.0.2
  shimmer_animation: ^2.1.0+1

  # State Management
  provider: ^6.0.4

  # Network
  dio: ^4.0.6

  # Localizations
  flutter_localizations:
    sdk: flutter

  # Format
  intl: ^0.17.0

  # Google Maps
  google_maps_flutter: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  generate: true
  uses-material-design: true
